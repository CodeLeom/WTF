// map, filter, reduce, find,findIndex, Arrow Functions

// MAP
// const newNum = numbers.map(item =>  item * 2 )
// console.log(newNum)

// FILTER
// const filterNumber = numbers.filter(num => num < 6)
// console.log(filterNumber)

var numbers = [3, 2, 6, 12]
// REDUCE
// let newNumbers = numbers.reduce(((acc, currValue) => acc + currValue))
// console.log(newNumbers)

// let newNumbers = numbers.reduce((acc, currValue) => acc + currValue, 10)
// console.log(newNumbers)

// CONDITIONAL RENDERING WITH TENARY OPERATOR (condition ? expression : expression) & AND OPERATOR

const isMarried = false

isMarried ? console.log('enjoy val with habibi') : console.log('watch second chance on telemundo') 

condition && expression